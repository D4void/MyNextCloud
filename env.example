# Copy this file as .env, chmod 600 it and edit variables
#

# Image version
NEXTCLOUD_TAG=31.0.6-apache
CODE_TAG=25.04.4.1.1
REDIS_TAG=8.0.2-alpine
MARIA_TAG=11.4-noble

# Differents FQDN used for Collabora Online / WOPI requests
NEXTCLOUD_FQDN=nextcloud.domain.fr
COLLABORA_FQDN=office.domain.fr
# COLLABORA_DOMAIN is used by domain environment variable. Dot must be escaped \\. (it's regex)
COLLABORA_DOMAIN=nextcloud\\.domain\\.fr

# Traefik IP required to force Nextcloud and Collabora discuss internally (cf extra_hosts in service)
#   not externally otherwise you need to set public ip in Nextcloud settings for "Allow list for Wopi requests"
TRAEFIK_IP=192.168.2.2

# Volume
NC_VOL=/opt/MyNextcloud
BKP_DIR=$NC_VOL/backup

# Login / Password
MARIADB_USER=ncuser
MARIADB_PASSWORD=ncpass
MARIADB_DATABASE=nextcloud
COLLABORA_USERNAME=collabuser
COLLABORA_PASSWORD=collabpass

# Backup
DATABACKUP=false
DATABACKUPUSER=username
DATABACKUPNAME=Backup-MyNextcloud